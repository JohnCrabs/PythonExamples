# Exercise Example: Simple Machine Learning
In this exercise, it is asked to develop a framework with the necessary error handling
intelligence in which it will read a json/yaml file of a data source file (CSV, XLS)
and will execute the training/validation/evaluation of several machine learning models 
(given in json/yaml file).

## Dependencies

